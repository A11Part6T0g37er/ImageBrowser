<Page
    x:Class="ImageBrowser.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
   >
    <Page.Resources>
      
        <Thickness x:Key="LargeItemMargin">8</Thickness>
        <Thickness x:Key="SmallItemMargin">0</Thickness>

        <ItemsPanelTemplate x:Key="ImageGridView_ItemsPanelTemplate">
            <ItemsWrapGrid Orientation="Horizontal"
                            ScrollViewer.VerticalScrollMode="Auto"
                           MaximumRowsOrColumns="5"/>
        </ItemsPanelTemplate>

        <CollectionViewSource x:Name="ImagesCollection" Source="{x:Bind imageFileInfoViewModel.GroupedImagesInfos}" 
                              IsSourceGrouped="True" />

        <CollectionViewSource x:Name="GropedImageCollection" Source="{x:Bind imageFileInfoViewModel.ObservableCollection}"
                              IsSourceGrouped="True"/>

        <Style x:Key="ImageGridView_DefaultItemContainerStyle"
               TargetType="GridViewItem">
            <Setter Property="Background"
                    Value="White" />
            <Setter Property="Margin"
                    Value="{StaticResource LargeItemMargin}" />
                     
        </Style>

        <Style x:Key="ImageGridView_SmallItemContainerStyle"
               TargetType="GridViewItem">
            <Setter Property="Background"
                    Value="Violet" />
            <Setter Property="Margin"
                    Value="{StaticResource SmallItemMargin}" />
        </Style>
        

        <DataTemplate x:Key="ImagesGridCollection_Small" x:DataType="local:ImageFileInfo">
            <Grid Margin="{StaticResource SmallItemMargin}" 
                  Width="60" Height="60">
                <Image x:Name="ItemImage" Stretch="UniformToFill"  Source="{Binding ImageFile.Path, Mode=OneWay}" 
                       HorizontalAlignment="Center" />
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="toolTipImage">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding ImageTitle, Mode=OneWay}" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding ImageFileType, Mode=OneWay}"/>
                                <TextBlock Text="{Binding ImageName, Mode=OneWay}" 
                                           HorizontalAlignment="Right" Margin="10,0,0,8"/>
                            </StackPanel>
                        </StackPanel>
                    </ToolTip>
                </ToolTipService.ToolTip>
                
            </Grid>
        </DataTemplate>
        
       
        
        <DataTemplate x:Key="ImagesGridCollection_Default" x:DataType="local:ImageFileInfo">
            <Grid Margin="{StaticResource LargeItemMargin}"
                   Height="{Binding ItemSize, ElementName=page}" >
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Image x:Name="ItemImage"  Stretch="UniformToFill" 
                      Source="{x:Bind ImageSource, Mode=OneWay}" 
                      Width="{Binding ItemSize,ElementName=app}" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,0"/>
                <StackPanel Orientation="Vertical" Grid.Row="1" Width="200">
                    <TextBlock Text="{x:Bind ImagePath, Mode=OneWay}"  Style="{StaticResource SubtitleTextBlockStyle}" />
                    
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Height="50">

                    
                            <ListView ItemsSource="{x:Bind ImageProperties.Keywords}" SelectionMode="Single" 
                                       VerticalAlignment="Center" Margin="0,0,50,0" >
                            
                                <ListView.ItemTemplate >
                               
                                    <DataTemplate x:Name="KeyWordds" x:DataType="local:ImageFileInfo" >
                                    <ListViewItem >

                                        <TextBlock  Text="{Binding}" FontSize="12"  />
                                    </ListViewItem>

                                    </DataTemplate>
                                
                            </ListView.ItemTemplate>
                            </ListView>
                        <ListView>
                            <TextBlock Text="{x:Bind ImageFile.Path}"/>
                            <TextBlock TextWrapping="WrapWholeWords" Text="{x:Bind ImageProperties.Height}"/>
                        </ListView>
                    </StackPanel>

                </StackPanel>
                <ToolTipService.ToolTip>
                    <ToolTip x:Name="toolTipImage">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="{Binding ImageTitle, Mode=OneWay}" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding ImageFileType, Mode=OneWay}"/>
                                <TextBlock Text="{Binding ImageName, Mode=OneWay}" 
                                           HorizontalAlignment="Right" Margin="10,0,0,8"/>
                                
                            </StackPanel>
                        </StackPanel>
                    </ToolTip>
                </ToolTipService.ToolTip>
                
            </Grid>
        </DataTemplate>

       
        
    </Page.Resources>

    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup>
            <VisualState x:Name="LargeWindowBreakpoint">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="{StaticResource LargeWindowBreakpoint}" />
                </VisualState.StateTriggers>
            </VisualState>
            <VisualState x:Name="MediumWindowBreakpoint">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="{StaticResource MediumWindowBreakpoint}" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="PicturesInGrid.ItemPanel"
                                Value="{StaticResource ImagesGridCollection_Default}" />
                    <Setter Target="ImageGridView.ItemContainerStyle"
                                Value="{StaticResource ImageGridView_DefaultItemContainerStyle}" />


                </VisualState.Setters>
            </VisualState>
            <VisualState x:Name="MinWindowBreakpoint">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="{StaticResource MinWindowBreakpoint}" />
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="PicturesInGrid.ItemPanel"
                                Value="{StaticResource ImagesGridCollection_Small}" />
                    <Setter Target="GroupedGrid.ItemPanel"  Value="{StaticResource ImagesGridCollection_Small}"/>
                    <Setter Target="ImageGridView.ItemContainerStyle"
                                Value="{StaticResource ImageGridView_SmallItemContainerStyle}" />
                    <Setter Target="GroupedGrid.ItemContainerStyle"
                                Value="{StaticResource ImageGridView_SmallItemContainerStyle}" />

                </VisualState.Setters>
            </VisualState>
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>




   
    <RelativePanel>
        <Pivot x:Name="leftControlPanel" Title="Image Browser" 
               Margin="25,0,0,15" RelativePanel.Below="TopButtons">
            <PivotItem Name="All" x:Uid="all" Header=""  >
                <StackPanel>
                <TextBlock x:Uid="allPictures" Text=""  />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind imageFileInfoViewModel.GroupedImagesInfos.Count, Mode=OneWay}"/>
                        <TextBlock x:Uid="groupedListDefinition" Text=""/>
                    </StackPanel>
                        <TextBlock x:Name="OneDriveInfo"/>
                </StackPanel>
            </PivotItem>

            <PivotItem x:Name="GroupedList" x:Uid="groupedList" Header=""  >
                <StackPanel Orientation="Vertical" Margin="0,0,100,0" >
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind imageFileInfoViewModel.GroupedImagesInfos.Count, Mode=OneWay}"/>
                        <TextBlock x:Uid="groupedListDefinition" Text=""/>
                    </StackPanel>
                    <ListView ItemsSource="{Binding Source={StaticResource ImagesCollection}}" RelativePanel.Below="button" 
                               Height="320" ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                              Background="WhiteSmoke">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem" BasedOn="{StaticResource ListViewItemExpanded}">
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ImageFileInfo">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{x:Bind ImageName, Mode=OneWay}" Style="{StaticResource BaseTextBlockStyle}" Grid.Row="0" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{x:Bind ImageDimensions, Mode=OneWay}" Grid.Row="1"/>
                                    <Image Source="{x:Bind ImageSource,Mode=OneWay}" Stretch="UniformToFill" Height="{Binding ItemSize,ElementName=page}" Grid.Row="2" Margin="0,5,0,10"/>

                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Key}" Style="{StaticResource TitleTextBlockStyle}"  FontWeight="Bold" FontSize="14" Foreground="Black" Margin="15,0"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ListView.GroupStyle>
                    </ListView>
                </StackPanel>
            </PivotItem>
            
        </Pivot>


        <CommandBar x:Name="TopButtons" DefaultLabelPosition="Right" IsOpen="True">
            <AppBarButton Icon="Refresh" x:Name="Refresh" Label="" x:Uid="refresh"  ToolTipService.ToolTip="" Click="Refresh_Click"/>
            <AppBarButton Icon="OpenFile" Label="" x:Uid="open" Click="ButtonOpen_Click"/>
        </CommandBar>

        <Grid x:Name="startingGreetingScreen" Visibility="Collapsed" 
                  RelativePanel.AlignVerticalCenterWithPanel="True" RelativePanel.AlignHorizontalCenterWithPanel="True"
              HorizontalAlignment="Center"
                    RelativePanel.Below="leftControlPanel" Margin="20,0,0,0" >
            
                    <TextBlock TextWrapping="Wrap" Text="Nothig here" />
                    <Image Source="/Assets/Square150x150Logo.scale-200.png" Margin="0,25,0,0" HorizontalAlignment="Right"/>
              
        </Grid>

        <RefreshContainer x:Name="RefreshArea" RelativePanel.Below="leftControlPanel" RelativePanel.AlignLeftWithPanel="True" 
                  RelativePanel.AlignRightWithPanel="True" RefreshRequested="RefreshArea_RefreshRequested">
           
          
        <GridView ItemsSource="{Binding Source={StaticResource ImagesCollection}}" SelectionMode="None"
                             Name="GroupedGrid" ItemsPanel="{StaticResource ImageGridView_ItemsPanelTemplate}"
                              SizeChanged="GroupedGrid_SizeChanged" 
               IsItemClickEnabled="True" ItemClick="GroupedGrid_ItemClick"
                  RelativePanel.Below="leftControlPanel" RelativePanel.AlignLeftWithPanel="True" 
                  RelativePanel.AlignRightWithPanel="True" MinWidth="180"  Margin="15">
            <GridView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Key}"  Style="{StaticResource SubtitleTextBlockStyle}" />
                        </DataTemplate>


                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </GridView.GroupStyle>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:ImageFileInfo" >
                    <Grid Margin="{StaticResource LargeItemMargin}"
                   Height="{Binding ItemSize, ElementName=page}" >
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>

                        </Grid.RowDefinitions>

                        <Image DataContextChanged="ItemImage_DataContextChanged" x:Name="ItemImage"  Stretch="Uniform" 
                      Source="{x:Bind ImageSource, Mode=OneWay}" 
                      Width="{Binding ItemSize,ElementName=app}" VerticalAlignment="Top" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,0"/>
                        
                        <StackPanel Orientation="Vertical" Grid.Row="1"  Margin="0,0,0,15">
                                <TextBlock Text="{x:Bind ImageName, Mode=OneWay}"  MaxWidth="500"
                                     TextWrapping="Wrap"  
                                           Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Left" VerticalAlignment="Top"/>

                        </StackPanel>
                            
                                <StackPanel Orientation="Horizontal" Grid.Row="2" >
                            <TextBlock TextWrapping="Wrap" Text="{x:Bind ImageDimensions, Mode=OneWay}" Margin="0,3,10,0" />
                          
                                    <ItemsControl ItemsSource="{x:Bind ImageProperties.Keywords, Mode=OneWay}" >
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapGrid Orientation="Horizontal"  MaximumRowsOrColumns="2" ItemWidth="40"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="x:String">
                                       
                                        <TextBlock   Text="{Binding}" FontSize="12"   TextWrapping="WrapWholeWords"/>
                                   
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>


                                </ItemsControl>

                            </StackPanel>
  
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
           
        </RefreshContainer>
        <StackPanel x:Name="OneDrivePanel" Orientation="Horizontal" RelativePanel.RightOf="TopButtons" 
                    RelativePanel.AlignTopWith="TopButtons" HorizontalAlignment="Center" 
                    RelativePanel.AlignRightWithPanel="True" Margin="50,0,0,0">

            <CommandBar DefaultLabelPosition="Right" IsOpen="True">
                <AppBarButton  Icon="Download" x:Name="signingOneDrive" Label="" x:Uid="signIn" 
                               ToolTipService.ToolTip="OneDrive" Click="SigningOneDrive_ClickAsync"/>
                <AppBarButton Icon="Account" x:Name="SignOutButton" Label="" x:Uid="signOut" 
                              Click="SignOutButton_ClickAsync"  Visibility="Collapsed" FontFamily="Segoe Ui"/>
            <AppBarButton x:Name="OpenOneDrive" Content="" x:Uid="oneDriveOpen" Visibility="Collapsed" Click="OpenOneDrive_Click"/>
                <CommandBar.Content >
                    <StackPanel>
                       
                        <TextBlock x:Name="ResultText"/>
                    </StackPanel>
                </CommandBar.Content>
                <CommandBar.SecondaryCommands>
                    <AppBarButton Icon="Setting" Label="" x:Uid="settings"/>
                    <AppBarButton Tag="Dark" x:Name="Dark" Label="" x:Uid="dark" Click="ThemeButton_Click"/>
                    <AppBarButton Tag="Light" x:Name="Light" Label="" x:Uid="light" Click="ThemeButton_Click"/>
                    <AppBarButton Tag="Default" x:Name="Default" Label="" x:Uid="default" Click="ThemeButton_Click"/>
                </CommandBar.SecondaryCommands>
            </CommandBar>
        </StackPanel>

    </RelativePanel>
</Page>
