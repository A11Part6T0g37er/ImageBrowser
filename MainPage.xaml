<Page
    x:Class="ImageBrowser.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ImageBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>


        <CollectionViewSource x:Name="ImageFileCollection" Source="{x:Bind ViewModel.ObservableCollection}"/>

    </Page.Resources>


    <Grid>
        <Pivot Title="Image Browser">
            <PivotItem Header="All">
                <TextBlock Text="all pictures go here." />
            </PivotItem>
            <PivotItem Header="Unsaved">
                <TextBlock Text="other pictures go here." />
            </PivotItem>

        </Pivot>


        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <Button  Content="Refresh"  ToolTipService.ToolTip="Refresh" Background="White" />
            <Button Content="Open"/>
        </StackPanel>

        <Image Stretch="UniformToFill" Source="Assets\backlit-photography-10.jpg" Width="190" Height="130"/>
        <GridView x:Name="PicturesInGrid" IsItemClickEnabled="True" ItemClick="PicturesInGrid_ItemClick" SelectionMode="Single" ItemTemplate="{StaticResource ImageFileCollection}"/>

        <Button Grid.Row="1" x:Name="button" Content="Hello, world!" HorizontalAlignment="Center" Margin = "150,60,0,0" VerticalAlignment="Top"/>
    </Grid>
</Page>
